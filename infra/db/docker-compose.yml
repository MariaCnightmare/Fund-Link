version: "3.9"

services:
  db:
    image: postgres:16
    container_name: fundlink-db
    restart: always
    env_file:
      - .env.example
    ports:
      - "5433:5432"
    volumes:
      - fundlink-data:/var/lib/postgresql/data

volumes:
  fundlink-data:
